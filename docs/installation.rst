============
Installation
============

At the command line::

    $ easy_install dj-channels-pubsub

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-channels-pubsub
    $ pip install dj-channels-pubsub
